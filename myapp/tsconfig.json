{
  "compilerOptions": {
    /* Cơ bản */
    "target": "ESNext",
    "module": "ESNext",
    "moduleResolution": "node",
    "lib": ["DOM", "DOM.Iterable", "ESNext"],

    /* React */
    "jsx": "react-jsx", // React 17+
    "allowJs": true, // Cho phép dùng .js song song với TS
    "checkJs": false, // Không bắt lỗi trong file .js (chỉ kiểm tra TS)
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,
    "skipLibCheck": true,

    /* Strict mode (bạn có thể bật/tắt tùy nhu cầu) */
    "strict": true,
    "forceConsistentCasingInFileNames": true,

    /* Đầu ra */
    "outDir": "./dist",
    "noEmit": true // Chỉ dùng cho dự án React (không build TS riêng)
  },
  "include": ["src"],
  "exclude": ["node_modules", "dist"]
}
